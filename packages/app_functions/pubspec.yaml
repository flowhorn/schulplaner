name: app_functions
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.2.0"
  flutter: ">=3.16.0"

dependencies:
  bloc:
    path: ../bloc
  cloud_functions: 4.5.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
